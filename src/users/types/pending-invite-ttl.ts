/** Unanswered (`pending`) invites expire and are deleted after this window. */
export const PENDING_INVITE_TTL_SECONDS = 24 * 60 * 60;
export const PENDING_INVITE_TTL_MS = PENDING_INVITE_TTL_SECONDS * 1000;
